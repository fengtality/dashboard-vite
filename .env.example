# Hummingbot Dashboard Configuration
# Copy this file to .env and update values as needed

# API Configuration
# In development, requests are proxied through Vite (leave as /api)
# In production, set the full Backend API URL
VITE_API_URL=/api

# Backend API Authentication
# These credentials must match the Backend API server configuration
VITE_API_USERNAME=admin
VITE_API_PASSWORD=admin

# Telegram Bot Integration (for Condor)
# Default bot username (without @) - users can change this in the Account page
VITE_TELEGRAM_BOT_USERNAME=condor_tg_bot

# Gateway Connection (through Hummingbot API proxy)
# Gateway is accessed through the API's /gateway-proxy/* endpoints
# Leave as default - the API forwards requests to Gateway
VITE_GATEWAY_URL=/api/gateway-proxy
# Optional API key for Gateway authentication (if Gateway requires it)
VITE_GATEWAY_API_KEY=
